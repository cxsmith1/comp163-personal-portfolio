import math

#Challenge 1
current_number = int(input('Enter starting number: '))
step_count = 0

while current_number > 1: #While the number entered is greater than our end goal, we run the loop
    if current_number % 2 != 1: #if input is divisible by 2 and can get down to one, divide by two (next line)
       current_number = int(current_number) / 2 
       print(current_number, end=' ')
       step_count += 1 #step count
    else:
        current_number = (int(n) * 3) + 1 #if input is odd, multiply by 3 and add one
        print(current_number, end=' ')
        current_number = (int(n) / 2) #divide by 2 after made even
        print(current_number, end=' ')
        step_count +=1 #step count

print (f'\nSteps: {step_count}') #total steps taken to get final answer

#Challenge 2

primefr = True #set to true unless proven otherwise
n = int(input('Enter a number: '))
newt = n-1 #creates end of range by subtracting one
print(f'Testing divisors from 2 to {newt}...') 

for i in range(2, newt): #creates range to search if prime
    if n % i == 0: #if n is divisible its not prime
        print(f'{n} is not prime (divisible by {i})')
        primefr = False #makes primefr false
            
if primefr: 
    print(f'{n} is prime!') 

print('')

# Challenge 3

#Challenge 3

goober = ''
collagen = ''
goobgen = ''

for goober in range(0, 11): #for the variable goober in the range 1-10
    for collagen in range(0, 11): #for each column in range 1-10
        goobgen = goober * collagen #creates our grid
        if goobgen < 10:
            bruh = "  " 
        else:
            if goobgen < 100: #if goobgen goes past our grid it wont print anything else
                bruh  = " " #space for numbers
        if collagen == 0: #if collagen is 0 start grid
            if goober == 0:#same here
                print("    ", end = '') #spaces out row
            else:
                print("  ", goober, end='') #ends row
        elif goober == 0:
            print("  ", collagen, end='')
        else:
            print(bruh, goobgen, end = '')
    print()

        
        
